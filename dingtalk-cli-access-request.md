# 钉钉 CLI（dws）访问权限申请

**申请人**：（填姓名 / 钉钉账号 / 手机号）
**申请日期**：2026-08-16
**需要管理员执行的操作**：在钉钉管理后台的 CLI 授权人员名单中，将申请人加入授权范围（目前登录时提示"您不在该组织的 CLI 授权人员范围内"）。

***

## 一、申请用途

我们团队在沉淀 CRO 行业知识库，并用 AI Agent（如 Codex 等）辅助日常工作。目前最大的问题是：**AI 的上下文里没有公司的组织架构**，理解业务知识时无法把"部门—角色—职责"对应起来，导致上下文分散、无法复用。

钉钉官方推出的 Workspace CLI（`dws`，开源地址：<https://github.com/DingTalk-Real-AI/dingtalk-workspace-cli> ）可以让 AI Agent 在获得授权后直接读取通讯录数据。接入后我们计划做两件事：

1. **组织结构进上下文**：通过 CLI 拉取部门树（`dws contact dept`）和企业角色列表（`dws contact label`），让 AI 在理解 CRO 行业知识时，能直接把行业角色（如 CRA、CRC、PM、医学、数统等）和公司部门、人员对应起来，形成可沉淀、可复用的统一上下文。
2. **新人自学**：新同事入职后装上钉钉 CLI，AI Agent 即可结合公司沉淀的文档 + 实时组织架构，帮助他们快速自学业务。

## 二、数据范围与使用方式

* **数据类型**：仅通讯录/组织架构相关（部门结构、成员列表、角色标签），用于知识库上下文构建。

* **操作性质**：以**只读查询**为主，不涉及修改组织架构、不涉及审批流操作。

* **使用方式**：员工本人在自己电脑上通过 OAuth 登录后使用，每次操作绑定本人身份。

## 三、安全性说明

dws 是钉钉官方开源工具，采用零信任架构：

* **OAuth 授权登录**：不落盘账号密码，凭证存于系统钥匙串（macOS Keychain）；

* **企业级管控**：CLI 访问需企业开通，且可按人员名单授权（即本次申请的事项）；

* **最小权限**：仅能访问管理员授权范围内的数据；

* **全程审计**：所有 CLI 操作有审计日志可查（`dws audit`）。

即：**没有任何数据能绕过企业认证和审计**，管理员随时可以撤销授权。

## 四、参考信息

* 项目地址：<https://github.com/DingTalk-Real-AI/dingtalk-workspace-cli>

* 项目目前处于官方共创阶段，如管理后台找不到 CLI 授权入口，可加入钉钉官方 DWS 共创群获取支持（项目 README 中有入口）。

***

管理员操作完成后，申请人重新执行 `dws auth login` 即可完成接入。
